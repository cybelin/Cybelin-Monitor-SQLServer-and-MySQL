<Window x:Class="WpfRequestResponseLogger.AddAlertEndpointWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Alert Endpoint" Height="200" Width="400">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Label and TextBox for Path -->
        <Label Grid.Row="0" Content="Path:"/>
        <TextBox Grid.Row="0" x:Name="txtPath" Margin="100,0,0,0" Width="250"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20,0,0">
            <Button x:Name="btnSave" Content="Save" Width="100" Margin="10" Click="BtnSave_Click"/>
            <Button x:Name="btnExit" Content="Exit" Width="100" Margin="10" Click="BtnExit_Click"/>
        </StackPanel>
    </Grid>
</Window>
